# Nix and friends

[![Ask DeepWiki](https://deepwiki.com/badge.svg)](https://deepwiki.com/with-lasers/nix)

The nix modules extracted from my configs

## Development

The `flake.nix` is generated by [flake-file].
When adding a `flake-file.inputs.<dependency>.url`, regenerate `flake.nix` with:

```sh
nix run .#write-flake
```

[flake-file]: https://flake-file.denful.dev/
